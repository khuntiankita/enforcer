{
  "name": "enforcer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix user",
    "backend": "npm run dev --prefix backend",
    "user": "npm start --prefix user",
    "dev": "npm-run-all --parallel backend user",
    "start": "npm-run-all --parallel backend user"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
